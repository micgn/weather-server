mqtt:
  broker.url: tcp://192.168.118.11:1883

logging:
  level:
    ROOT: INFO
    de.mg.wather.server: INFO

spring:
  datasource:
      url: jdbc:hsqldb:file:~/hsqldb/weather/weather
      username: sa
      password:
      min-idle: 2
      max-idle: 2
      max-active: 5
      test-on-borrow: true
      validation-query: SELECT 1
    jpa.hibernate.ddl-auto: update
    jpa.properties.hibernate.dialect: org.hibernate.dialect.HSQLDialect
